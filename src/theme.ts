import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
          main: '#34428c',
        },
        secondary: {
          main: '#51aec9',
        },
        background: {
          default: '#070720',
        }
    }
});


export default theme